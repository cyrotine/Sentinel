[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "forge-github"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.10",
  "PyGithub>=2.4",
  "httpx>=0.27",
]

[tool.hatch.build.targets.wheel]
packages = ["forge_github"]
