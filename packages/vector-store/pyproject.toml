[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "forge-vector-store"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.10",
  "qdrant-client>=1.12",
]

[tool.hatch.build.targets.wheel]
packages = ["forge_vector_store"]
